# Day 9 — 收尾与对外展示 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将 Socratic AI MVP 仓库打磨为可对外展示状态（面试/Portfolio），含完整 README、代码清理、质量验证、GitHub Push。

**Architecture:** 分三阶段：代码清理（debugPrint 替换 + 死代码移除 + pubspec 审查）→ README 重写（架构图 + 技术栈 + 截图占位 + 简历话术）→ 最终验证与推送。

**Tech Stack:** Flutter 3.x, Dart, Markdown, Git, GitHub

---

## 前置检查

当前项目状态：

```
lib/              35 个 Dart 文件，0 error 0 warning
test/             17 个测试文件
debugPrint 调用   20+ 处（分布在 5 个文件）
注释掉的代码      1 处（topic_card.dart:17 doc comment 示例）
pubspec           版本 1.0.0+1，描述已基本完善
```

---

### Task 1: 替换所有 debugPrint 为统一日志方案

**背景**：当前 20+ 处 `debugPrint()` 散落在 5 个文件中，对外仓库不应有 debug 日志裸奔，需统一替换为条件编译或 remove。

**方案**：创建轻量 `AppLogger`，默认 `kReleaseMode` 下静默，debug 模式打印。

**Files:**
- Create: `lib/core/logger.dart`
- Modify: `lib/core/engine/llama_service.dart` (6 处)
- Modify: `lib/features/chat/providers/chat_provider.dart` (3 处)
- Modify: `lib/features/chat/engine/socratic_prompter.dart` (5 处)
- Modify: `lib/features/mindmap/engine/graph_service.dart` (3 处)
- Modify: `lib/features/insights/engine/insight_service.dart` (3 处)
- Modify: `lib/features/history/providers/history_provider.dart` (3 处)

**Step 1: 创建 AppLogger**

```dart
import 'package:flutter/foundation.dart';

/// 统一日志工具，release 模式静默，debug 模式输出
class AppLogger {
  const AppLogger._();

  static void info(String tag, String message) {
    if (!kReleaseMode) {
      debugPrint('[$tag] $message');
    }
  }

  static void warn(String tag, String message) {
    if (!kReleaseMode) {
      debugPrint('⚠️ [$tag] $message');
    }
  }

  static void error(String tag, String message, [Object? e]) {
    if (!kReleaseMode) {
      debugPrint('❌ [$tag] $message${e != null ? ': $e' : ''}');
    }
  }
}
```

**Step 2: 替换所有 debugPrint 调用**

替换规则（按 tag 分组）：

| 文件 | tag | debugPrint 内容 | 替换为 |
|------|-----|----------------|--------|
| `llama_service.dart` | `LlamaService` | "引擎加载完成: ..." | `AppLogger.info('LlamaService', '引擎加载完成: ${config.modelPath}')` |
| `llama_service.dart` | `LlamaService` | "正在释放 ..." | `AppLogger.info('LlamaService', '正在释放 ${_pool.length} 个引擎...')` |
| `llama_service.dart` | `LlamaService` | "引擎已释放" | `AppLogger.info('LlamaService', '引擎已释放')` |
| `llama_service.dart` | `LlamaService` | "正在加载引擎: ..." | `AppLogger.info('LlamaService', '正在加载引擎: ${config.modelPath}')` |
| `llama_service.dart` | `LlamaService` | "引擎启动完成" | `AppLogger.info('LlamaService', '引擎启动完成')` |
| `llama_service.dart` | `LlamaService` | "加速器: ..." | `AppLogger.info('LlamaService', '加速器: ${engine.primaryAcceleratorName}')` |
| `llama_service.dart` | `LlamaService` | "引擎加载失败: ..." | `AppLogger.error('LlamaService', '引擎加载失败', e)` |
| `chat_provider.dart` | `ChatProvider` | "模型加载失败..." | `AppLogger.warn('ChatProvider', '模型加载失败', e)` |
| `chat_provider.dart` | `ChatProvider` | "推理失败: ..." | `AppLogger.error('ChatProvider', '推理失败', e)` |
| `chat_provider.dart` | `ChatProvider` | "洞察生成失败: ..." | `AppLogger.error('ChatProvider', '洞察生成失败', e)` |
| `socratic_prompter.dart` | `SocraticPrompter` | "初始化失败: ..." | `AppLogger.error('SocraticPrompter', '初始化失败', e)` |
| `socratic_prompter.dart` | `SocraticPrompter` | 其他 4 处 | 按语义映射到 info/warn/error |
| `graph_service.dart` | `GraphService` | 3 处 | 映射到 info/error |
| `insight_service.dart` | `InsightService` | 3 处 | 映射到 info/error |
| `history_provider.dart` | `HistoryProvider` | 3 处 | 映射到 error |

**Step 3: 验证**

```bash
grep -rn "debugPrint\(" lib/ --include="*.dart"
```
期望输出：**仅 `lib/core/logger.dart` 中包含 debugPrint**（AppLogger 内部实现）。

**Step 4: 运行 flutter analyze**

```bash
flutter analyze lib/
```
期望：0 error, 0 warning

**Step 5: Commit**

```bash
git add lib/core/logger.dart
git add lib/core/engine/llama_service.dart
git add lib/features/chat/providers/chat_provider.dart
git add lib/features/chat/engine/socratic_prompter.dart
git add lib/features/mindmap/engine/graph_service.dart
git add lib/features/insights/engine/insight_service.dart
git add lib/features/history/providers/history_provider.dart
git commit -m "chore: replace debugPrint with conditional AppLogger for release builds"
```

---

### Task 2: 清理死代码、注释掉的代码、过期注释

**Files:** 逐个检查

**Step 1: topic_card.dart — 移除注释掉的示例代码**

文件：`lib/features/topics/widgets/topic_card.dart`
定位第 17 行（doc comment 内注释掉的 print 示例）：

旧：
```dart
///   onTap: (topic) => print('选了 ${topic.title}'),  // 点击回调
```

改为：删除整行（或保留不含示例代码的 doc），例如：
```dart
///   onTap: (topic) { /* handle selection */ },
```

**Step 2: dialogue_engine.dart — 移除 doc comment 中的示例 print**

文件：`lib/core/engine/dialogue_engine.dart`
定位 `print(token)` 示例行，改为不含代码的描述。

**Step 3: 全量扫描确认无遗漏**

```bash
# 检查注释掉的代码（如 // print(, // TODO, /* ... */ 死代码）
grep -rn "// print\|// TODO\|// FIXME\|// HACK\|//.*debugPrint\|/\*.*\*/" lib/ --include="*.dart"
```

期望：无输出（除了注释中的合理说明）。

**Step 4: Commit**

```bash
git add lib/features/topics/widgets/topic_card.dart
git add lib/core/engine/dialogue_engine.dart
git commit -m "chore: remove dead code and commented-out debug examples"
```

---

### Task 3: pubspec.yaml 审查与补充

**Files:**
- Modify: `pubspec.yaml`

**Step 1: 补充必要字段**

当前状态：
```yaml
name: socratic_ai
description: "苏格拉底式 AI 对话 - 一个通过追问帮你理清思路的 AI 对话 App。"
version: 1.0.0+1
```

需要补充：
- `homepage` / `repository` — GitHub 地址
- 描述更丰富（体现端侧 AI 特色）

修改为：
```yaml
name: socratic_ai
description: >-
  苏格拉底式 AI 对话 — 端侧推理、隐私零上传。
  通过 Qwen 1.5B 端侧 LLM 驱动的追问、洞察总结与思维图谱，
  帮助用户理清思路、发现认知盲区。
version: 1.0.0+1
repository: https://github.com/SuperJieLab/socratic-ai
publish_to: 'none'
```

**Step 2: Commit**

```bash
git add pubspec.yaml
git commit -m "chore: polish pubspec.yaml for public display"
```

---

### Task 4: README.md 重写

**Files:**
- Modify: `README.md`

这是 Day 9 的核心产出，目标：让面试官 / GitHub 访客在 3 分钟内知道这个项目是什么、怎么跑、为什么厉害。

**Step 1: 阅读当前 README 作为参考**

**Step 2: 重写结构**

```markdown
# Socratic AI

> 反过来的聊天机器人：AI 追问你，而不是你问 AI。
> 端侧推理，隐私零上传。

[![Flutter](https://img.shields.io/badge/Flutter-3.x-02569B?logo=flutter)](https://flutter.dev)
[![Dart](https://img.shields.io/badge/Dart-3.x-0175C2?logo=dart)](https://dart.dev)
[![License](https://img.shields.io/badge/license-MIT-green)](LICENSE)

## 一句话

通过苏格拉底式追问帮你理清思路的 AI 对话 App —— AI 是提问者，你是回答者。

## 为什么做这个

99% 的 AI 产品是「你问它答」。我们做反向的——AI 追问你的底层假设、帮你发现思维盲区。
- 传统 Chatbot → 给你答案，越用越依赖
- Socratic AI → 帮你提问，越用越有独立思考能力

## 核心功能

| 功能 | 说明 |
|------|------|
| 🎯 话题选择 | 5 个预设话题 + 自定义输入 |
| 💬 多轮追问 | 梯度追问策略（探索→深入→挑战→总结），适应短/长回答 |
| 🔍 洞察总结 | 端侧 LLM 分析全文，生成洞察卡片 + 价值观标签 + 矛盾高亮 |
| 🧠 思维图谱 | 端侧 LLM 生成图谱 → 辐射布局 → 可交互可视化（缩放/拖拽） |
| 📂 历史列表 | 本地持久化，支持收藏和删除 |

## 技术亮点

### 端侧推理，零隐私上传
- 对话全程在设备端完成，使用 llama.cpp + Qwen 2.5 1.5B (Q4_K_M, ~1.06GB)
- iOS 自动启用 CoreML 加速，Android 自动启用 NNAPI
- 对话原始内容不离开设备，只有脱敏后的图谱结构可同步

### Dart FFI 统一双端
- 一份 Dart 代码桥接 C++ 推理引擎，无需 Swift/Kotlin Native 代码
- 梯度追问策略（探索→深入→挑战→总结）+ 去重检测在 Dart 层完成

### 自研图谱布局
- 端侧 LLM 直接输出图谱 JSON 结构
- Dart 端自研辐射分布布局算法（无需 dagre/Graphviz 等外部库）
- Flutter CustomPainter 渲染 + 手势交互（缩放/拖拽/点击）

## 技术栈

| 层 | 技术 | 用途 |
|------|------|------|
| UI 框架 | Flutter 3.x | 跨平台 iOS + Android |
| 状态管理 | Provider | 对话 + 推理状态 |
| 推理引擎 | llama.cpp (C++) | C++ 编译进 App |
| Dart FFI | llama_cpp_dart | Dart ↔ C++ 推理调用 |
| 模型 | Qwen 2.5 1.5B (Q4_K_M) | 中文苏格拉底对话 |
| 加速 | CoreML (iOS) / NNAPI (Android) | 自动启用 |
| 持久化 | sqflite | SQLite 本地存储 |
| 图谱布局 | 自研辐射分布 | Dart 原生算法 |
| 图谱渲染 | CustomPainter | Flutter 渲染引擎 |

## 架构

```
┌─────────────────────────────────────────┐
│           Flutter UI (Dart)             │
│  话题选择 │ 对话界面 │ 洞察总结 │ 思维图谱  │
│              │ Provider                 │
│   ┌──────────┴──────────┐               │
│   │  Dart FFI 桥接       │               │
│   │  llama_cpp_dart     │               │
│   └──────────┬──────────┘               │
│   ┌──────────┴──────────┐               │
│   │  llama.cpp (C++)    │               │
│   │  Qwen 2.5 1.5B      │               │
│   │  CoreML / NNAPI     │               │
│   └─────────────────────┘               │
├─────────────────────────────────────────┤
│  sqflite — 本地持久化                    │
│  conversations / messages / graphs       │
└─────────────────────────────────────────┘
```

## 快速开始

### 前置条件
- Flutter 3.x
- Xcode (iOS/macOS) 或 Android SDK
- ~2GB 空闲存储（模型文件 ~1.06GB）

### 运行
```bash
git clone https://github.com/SuperJieLab/socratic-ai.git
cd socratic-ai
flutter pub get

# 下载模型（需要从 HuggingFace 下载）
# 详见 docs/demo-plan-socratic-ai.md 的Day 8部分

flutter run
```

### 测试
```bash
flutter test
flutter analyze
```

## 截图

> 运行应用后截图，替换以下占位符

| 话题选择 | 对话中 | 洞察总结 |
|:---:|:---:|:---:|
| (占位) | (占位) | (占位) |

| 思维图谱 | 历史列表 | 错误状态 |
|:---:|:---:|:---:|
| (占位) | (占位) | (占位) |

## 简历落点

> 独立开发基于 Flutter + llama.cpp 的苏格拉底式 AI 对话应用，通过 Dart FFI
> 桥接 Qwen 2.5 1.5B 端侧推理引擎，Metal/CoreML 自动加速；端侧 LLM 驱动
> 追问策略、洞察总结与思维图谱生成；完整数据本地持久化，隐私零上传；
> 一个代码库覆盖双端从 UI 到推理的完整链路。

## 面试话术速查

| 面试官问 | 回答方向 |
|------|------|
| 「为什么是苏格拉底式？」 | 反向设计——AI 追问用户假设，帮助独立思考，不是给答案 |
| 「1.5B 够用吗？」 | 追问比回答简单，需要的是分析上一条回答的能力而非知识广度 |
| 「为什么 Dart FFI 不用 MethodChannel？」 | 一份代码双端共享，无需 Swift + Kotlin 两套 Native 代码 |
| 「思维图谱怎么做的？」 | 端侧 LLM → JSON 结构 → 辐射分布布局 → CustomPainter 渲染 |
| 「隐私怎么保证？」 | 对话在端侧推理，不上传。后端只存脱敏结构化数据 |

## 项目状态

MVP 核心功能完成（Day 1-7），Day 8 模型下载开发中。

后续计划：
- v1.1: 话题推荐 + 语音输入
- v1.2: Watch 端触发 + 心率感知 + Web 端回顾台
- 高阶版: Go 后端 + 多端同步 + 用户系统

## License

MIT
```

**Step 3: Commit**

```bash
git add README.md
git commit -m "docs: rewrite README for public portfolio display"
```

---

### Task 5: 最终质量验证

**Step 1: flutter analyze 全项目**

```bash
flutter analyze lib/ test/
```
期望：0 error, 0 warning（可接受少量 info 级 lint，如 avoid_print 仅限 test/）。

**Step 2: flutter test**

```bash
flutter test
```
记录结果：通过数/总数，失败的注明原因（如集成测试需真实硬件）。

**Step 3: 检查是否有遗漏文件需要 .gitignore**

```bash
git status
```
确认无 .DS_Store、build/、临时文件在暂存区。

**Step 4: 最终 diff review**

```bash
git diff HEAD~N --stat   # N = 从上次 Push 到现在的 commit 数
```

---

### Task 6: GitHub Push

**Step 1: 确认远端**

```bash
git remote -v
```
期望：`origin  git@github.com:SuperJieLab/socratic-ai.git`

**Step 2: Push**

> ⚠️ 不自动执行，等用户确认后手动 push。
> 由用户提供 push 指令。

```bash
git push origin main
```

---

## 验收标准 Checklist

- [ ] `flutter analyze lib/` → 0 error, 0 warning
- [ ] 全项目无 `debugPrint` 裸调用（仅 `AppLogger` 内部）
- [ ] 无注释掉的代码、无 TODO/FIXME/HACK
- [ ] `pubspec.yaml` 含 repository 和完整描述
- [ ] `README.md` 含架构图、技术栈、快速开始、简历话术
- [ ] 代码 Push 到 GitHub（public）
- [ ] 截图准备说明已提供（在 README 中有占位标记）

---

## 非代码任务（后续手动完成）

这些任务需要真机运行，无法在当前环境完成，列在此处供后续执行：

1. **截图 6 张页面**（iPhone 镜像或模拟器）
   - 话题选择页
   - 对话中（AI 提问 + 用户回答）
   - 洞察总结页
   - 思维图谱页
   - 历史列表页
   - 错误状态（如模型未加载）
   
   推荐工具：`flutter screenshot` 或 iPhone 镜像截屏

2. **录制 2 分钟 Demo 视频**
   - 完整流程：选话题 → 3-4 轮追问 → 查看洞察 → 查看图谱 → 返回历史
   - 推荐工具：iPhone 录屏 + 简单的旁白

3. **截图/视频更新到 README**
   - 替换 README 中的 6 个占位标记为实际图片

4. **（可选）App 图标替换**
   - 当前为 Flutter 默认图标
   - 可用 `flutter_launcher_icons` 包批量生成各尺寸

5. **（可选）License 文件**
   - 添加 `LICENSE` 文件（推荐 MIT）

---

## 执行选项

**Plan complete and saved to `docs/plans/2026-07-10-day9-wrap-up.md`. Two execution options:**

**1. Subagent-Driven (this session)** — I dispatch fresh subagent per task, review between tasks, fast iteration

**2. Sequential (this session)** — I execute tasks one by one in order, reviewing each commit

**Which approach?**
